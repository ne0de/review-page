{{#if account}}
<div>
  <div class="row no-gutters">
    <div class="col-md-4">
      <img src="/avatar.png" class="card-img" alt="...">
    </div>
    <div class="col-md-8">
      <div class="card-body" style="height: 100%;">
        <h4 class="text-white">{{account.name}} {{account.surname}}</h4>
        <p class="card-text"><small class="text-muted">#{{account.nickname}}</small></p>
        <div style="height:70%">
          <p class="text-white">No hay ninguna descripción disponible.</p>
        </div>
        <p class="card-text"><small class="text-muted">{{account.createdAt}}</small></p>
      </div>
    </div>
  </div>
</div>
{{/if}}